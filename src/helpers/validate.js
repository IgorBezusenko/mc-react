export  function validRequired(text) {
    return `Поле "${text}" объязательно для заполнения`
}
export  function validLink(text) {
    return `Поле "${text}" должно быть ссылкой`
}

export  function validBirthday(text) {
    return `Поле "${text}" не должно превышать текуший год`
}

